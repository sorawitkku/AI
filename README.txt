For algorithm that I used it's called Minimax (use wiki for peudocode base) depth 3 
because if more that is it's can't run within 1 sec with 10 x 10 so pls teacher don't make bigger state that is plsss ;-; 
the scoring method that I use when minimax call recursion until depth equal to 0 are
if AI can make X in vertical, horisental, diagnol will get 100 point
and X - 1 will get 10 and Opponent can make X-1 score will be -90 to prevent it to happen